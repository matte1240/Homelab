# Proxmox credentials
packer/credentials.pkr.hcl

# SSH keys
*.pem
*.pub
id_rsa*

# Packer
packer_cache/
*.box

# Temporary files and logs
*.tmp
*.log
crash.log

# Editors / IDEs
.vscode/

# OS files
.DS_Store

# Linux temporary/backups
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# Terraform - consolidated rules (applies to any terraform directory)
# Ignore Terraform state, plans, lock and variable files
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
terraform.tfvars

# Ansible - consolidated rules
# Ansible vault and sensitive files
vault.yml
vault_pass.txt
*.vault

# Ansible retry files and logs
*.retry
ansible.log

# Ansible Galaxy collections and roles (if locally downloaded)
collections/ansible_collections/
roles/*/

# Host-specific variables (may contain sensitive data)
host_vars/*/vault.yml

# Ansible temporary files
.ansible/
